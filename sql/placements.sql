SELECT STUDENTS.NAME FROM FRIENDS JOIN PACKAGES ON FRIENDS.FRIEND_ID = PACKAGES.ID JOIN STUDENTS ON FRIENDS.ID = STUDENTS.ID WHERE PACKAGES.SALARY > (SELECT SALARY FROM PACKAGES WHERE STUDENTS.ID = PACKAGES.ID) ORDER BY PACKAGES.SALARY ASC; 